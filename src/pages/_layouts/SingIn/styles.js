import styled from 'styled-components';

import {blue_area} from '~/styles/Colors';

export const Wrapper = styled.section`
  background-color: ${blue_area};
  height: 100%;
  display: flex;
  align-items: center;
`;
